# This aims to be the source of truth for packages to install in an ubuntu environment 
# install for dev.py and installers, so we don't have a list in a script somewhere.

# Utilities
bash
grep
sed
coreutils
zip
unzip
p7zip-full
curl
jq

# Python
python3
python3-pip
python3-tk
python3-pil
python3-pil.imagetk

# Java
openjdk-17-jre
openjdk-8-jre

# Bioinformatics
samtools
bcftools
tabix
bwa
bowtie2
minimap2
fastp
